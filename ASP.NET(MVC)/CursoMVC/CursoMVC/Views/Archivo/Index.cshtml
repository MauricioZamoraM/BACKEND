@*Esto significa que la vista espera recibir un objeto del tipo ArchivoViewModel como modelo de datos.*@
@*En el ArchivoViewModel estan las propiedades con sus respectivos get y set para que puedan ser obtenidas y modificadas.*@
@model  CursoMVC.Models.ViewModels.ArchivoViewModel

@*Se establece el título de la página*@
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@*Verifica si la propiedad ViewBag.Message tiene algún valor*@
@if (ViewBag.Message != null)
{
    <div class="alert alert-success role=alert">
        @ViewBag.Message
    </div>
}
@*se utiliza el helper Html.BeginForm para generar un formulario HTML que enviará los datos a la acción "Save" del controlador "Archivo" utilizando el método HTTP POST y el atributo enctype establecido en "multipart/form-data" para permitir el envío de archivos binarios.*@
@using (Html.BeginForm("Save", "Archivo", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken();
    <div class="row">
        <div class="col-md-6 col-lg-offset-3">
            <div class="form-group">
                @*El @Html permite usar las propiedades de modelo ArchivoViewModel*@
                @Html.LabelFor(d => d.Archivo1)
                @Html.TextBoxFor(d => d.Archivo1, new { @class = "form-control", type = "file", accept = ".png" })
                @Html.ValidationMessageFor(d => d.Archivo1, "", new { @class = "text-danger" })
            </div>
            <div class="form-group">
                @Html.LabelFor(d => d.Archivo2)
                @Html.TextBoxFor(d => d.Archivo2, new { @class = "form-control", type = "file", accept = ".png" })
                @Html.ValidationMessageFor(d => d.Archivo2, "", new { @class = "text-danger" })
            </div>
            <div class="form-group">
                @Html.LabelFor(d => d.Cadena)
                @Html.TextBoxFor(d => d.Cadena, new { @class = "form-control" })
                @Html.ValidationMessageFor(d => d.Cadena, "", new { @class = "text-danger" })
            </div>
            <div class="form-group" style="text-align:right;">
                <input type="submit" class="btn btn-success" value="Guardar" />

            </div>

        </div>
    </div>
}